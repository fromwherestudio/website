import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Mettre l'URL finale du site ici une fois le domaine choisi (utile pour le SEO / sitemap).
  // site: 'https://www.fromwherestudio.be',
});
